type Model = {
    image: string
    usdzFile: string
    status: string
    localizedNames: Record<string, string | undefined>
    dxfFile: string
    props: any
    modelFile: string
    category: string
    name: string
}

export default Model